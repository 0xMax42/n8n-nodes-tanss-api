{
    "name": "updateRemoteSupport returns success wrapper",
    "itemIndex": 0,
    "parameters": {
        "operation": "updateRemoteSupport",
        "remoteSupportId": 5,
        "remoteObject": { "comment": "Updated" }
    },
    "credentials": {
        "tanssApi": {
            "baseURL": "https://example.invalid",
            "authentication": "apiToken",
            "apiToken": "TOKEN-123"
        }
    },
    "http": [
        {
            "expect": {
                "method": "PUT",
                "url": "https://example.invalid/backend/api/remoteSupports/v1/5",
                "json": true,
                "headers": {
                    "Content-Type": "application/json",
                    "apiToken": "Bearer TOKEN-123"
                },
                "body": { "comment": "Updated" }
            },
            "reply": {
                "statusCode": 200,
                "body": { "id": 5, "comment": "Updated" },
                "headers": { "content-type": "application/json" }
            }
        }
    ],
    "expected": {
        "kind": "returns",
        "matchObject": { "success": true, "statusCode": 200 }
    }
}
