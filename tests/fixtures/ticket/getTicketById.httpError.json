{
    "name": "getTicketById returns http-error wrapper",
    "itemIndex": 0,
    "parameters": { "operation": "getTicketById", "ticketId": 1 },
    "credentials": {
        "tanssApi": {
            "baseURL": "https://example.invalid",
            "authentication": "apiToken",
            "apiToken": "TOKEN-123"
        }
    },
    "http": [
        {
            "expect": {
                "method": "GET",
                "url": "https://example.invalid/backend/api/v1/tickets/1"
            },
            "throw": {
                "kind": "axios",
                "statusCode": 404,
                "data": { "message": "not found" }
            }
        }
    ],
    "expected": {
        "kind": "returns",
        "matchObject": {
            "success": false,
            "statusCode": 404,
            "body": { "message": "not found" }
        }
    }
}
