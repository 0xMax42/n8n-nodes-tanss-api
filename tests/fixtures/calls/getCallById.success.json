{
    "name": "getCallById returns success wrapper",
    "itemIndex": 0,
    "parameters": {
        "operation": "getCallById",
        "phoneCallId": 42
    },
    "credentials": {
        "tanssApi": {
            "baseURL": "https://example.invalid",
            "authentication": "apiToken",
            "apiToken": "TOKEN-123"
        }
    },
    "http": [
        {
            "expect": {
                "method": "GET",
                "url": "https://example.invalid/backend/api/calls/v1/42",
                "json": true,
                "headers": {
                    "apiToken": "Bearer TOKEN-123"
                }
            },
            "reply": {
                "statusCode": 200,
                "body": { "id": 42 },
                "headers": { "content-type": "application/json" }
            }
        }
    ],
    "expected": {
        "kind": "returns",
        "matchObject": {
            "success": true,
            "statusCode": 200,
            "body": { "id": 42 }
        }
    }
}
